import styled from "styled-components";

const IndicatorWapper = styled.div`
	position: relative;
	overflow: hidden;
	width: 30%;

	.i-content {
		display: flex;
		transition: all 200ms ease;
	}
`;

export default IndicatorWapper;
